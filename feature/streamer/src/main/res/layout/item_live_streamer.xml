<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="actionHandler"
            type ="com.hegunhee.feature.streamer.StreamActionHandler"
            />

        <variable
            name="streamData"
            type="com.hegunhee.domain.model.StreamDataType.StreamData" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{() -> actionHandler.onClickTwitchStreamerItem(streamData.userLogin)}">

        <ImageView
            android:id="@+id/thumbNailImage"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:setImage="@{streamData.thumbnailUrl}"
            />
        
        <TextView
            android:id="@+id/viewer_counter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="@id/thumbNailImage"
            app:layout_constraintBottom_toBottomOf="@id/thumbNailImage"
            android:layout_marginStart="4dp"
            android:layout_marginBottom="4dp"
            android:textSize="15dp"
            android:text="@{streamData.viewerCount}"
            android:textColor="@color/white"
            />

        <ImageView
            android:id="@+id/profileImage"
            android:layout_width="30dp"
            android:layout_height="30dp"
            app:setImage="@{streamData.thumbnailUrl}"
            android:layout_marginStart="10dp"
            app:layout_constraintTop_toTopOf="@id/thumbNailImage"
            app:layout_constraintStart_toEndOf="@id/thumbNailImage"/>

        <TextView
            android:id="@+id/streamerName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/profileImage"
            app:layout_constraintStart_toEndOf="@id/profileImage"
            android:gravity="center"
            android:text="@{streamData.userName}"
            android:textSize="20dp"
            android:layout_marginStart="4dp"
            />

        <TextView
            android:id="@+id/streamTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="@id/profileImage"
            app:layout_constraintTop_toBottomOf="@id/profileImage"
            android:layout_marginTop="4dp"
            android:gravity="center"
            android:text="@{streamData.title}"
            android:textSize="20dp"
            android:layout_marginStart="4dp"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>
